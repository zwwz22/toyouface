
<table>
  <tr>
    <th>name</th>
    <th>关注用户</th>
  </tr>
  <% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.name %>111</td>
    <td><%= user.name %>222</td>
    <td><%= user.name %>333</td>
  </tr>
  <% end %>
</table>
<%= collection_select(:user,:a,User.all,:id,:name)%>
<%= select('user','b',User.all.collect{|u|[u.name,u.id]}) %>
<%= select('user','c',[[1,3],[4,6]]) %>
<%= collection_select(:user,:id,User.all,:id,:name) %>